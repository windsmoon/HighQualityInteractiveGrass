// shadow acne :
// increasing the atlas size decreases the world-space size of the texels, so the acne artifacts get smaller
// however, the amount of artifacts also increases, so the problem cannot be solved by simply increasing the atlas size

// the normal bias is recommended to use 1 as default value, the slope scale bias is 0